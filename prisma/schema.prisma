// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}


model User {
  id          String    @id @default(uuid()) @db.Uuid
  username    String    @unique
  password    String
  protocolId  String    @unique @db.Uuid
  status      Boolean   @default(true)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  
  character   Character[]
  protocol    Protocol  @relation(fields: [protocolId], references: [id])
}

model Protocol {
  id          String    @id @default(uuid()) @db.Uuid
  createdAt   DateTime  @default(now())

  user        User?
}

model Character {
  id              String @id @default(uuid()) @db.Uuid
  name            String @db.VarChar(80)
  surname         String @db.VarChar(255)
  xp              Int    @default(0)
  user            User   @relation(fields: [userId], references: [id])
  userId          String @db.Uuid
  background      String
  createdAt       DateTime   @default(now())
  updatedAt       DateTime   @updatedAt

  campaigns       CampaignCharacter[]
  baseAttributes  CharacterBaseAttributes[]
  attributes      CharacterAttributes[]
}

model Campaign {
  id          String @id @default(uuid()) @db.Uuid
  name        String @db.VarChar(80)
  master      String @db.VarChar(255)
  background  String
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  
  characters  CampaignCharacter[]
  attributes  AttributesCampaign[]
}

model ClassRpg {
  id            String @id @default(uuid()) @db.Uuid
  name          String @db.VarChar(40)
  description   String
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}

model CampaignCharacter {
  charId      String    @db.Uuid
  campaignId  String    @db.Uuid
  finalDesc   String
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  character   Character @relation(fields: [charId], references: [id])
  campaign    Campaign  @relation(fields: [campaignId], references: [id])
  @@id([charId, campaignId])
}

model Attributes {
  id              String @id @default(uuid()) @db.Uuid
  name            String @db.VarChar(80)
  description     String
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  
  baseAttributes  CharacterBaseAttributes[]
  attributes      CharacterAttributes[]
  campaigns       AttributesCampaign[]
}

model AttributesCampaign {
  attrId      String      @db.Uuid
  campaignId  String      @db.Uuid
  value       Int?      
  status      Boolean     @default(true)
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  attribute   Attributes  @relation(fields: [attrId], references: [id])
  campaign    Campaign    @relation(fields: [campaignId], references: [id])
  @@id([attrId, campaignId])
}

model CharacterBaseAttributes {
  charId      String      @db.Uuid
  attrId      String      @db.Uuid
  value       Int
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  character   Character   @relation(fields: [charId], references: [id])
  attribute   Attributes  @relation(fields: [attrId], references: [id])
  @@id([charId, attrId])
}

model CharacterAttributes {
  charId      String      @db.Uuid
  attrId      String      @db.Uuid
  value       Int
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  character   Character   @relation(fields: [charId], references: [id])
  attribute   Attributes  @relation(fields: [attrId], references: [id])
  @@id([charId, attrId])
}